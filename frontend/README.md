# Frontend (Streamlit)

This folder hosts the Streamlit UI for the NASA RAG app.

## Start UI

```bash
uv run streamlit run frontend/app.py
```

## Notes

- Backend API must be running first (default: http://localhost:8000).
- You can change API base URL from the Streamlit sidebar.
